// const router = require('express').Router();
// const versions = {
//   '1': require('./v1')
// };

// const api = new Router();

// api.use('/v1', versions['1'].routes());

// module.exports = api;