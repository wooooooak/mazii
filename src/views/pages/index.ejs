<!DOCTYPE html>
<html>
<head>
    <title>mazii</title>
    <% include ../partials/stylesheet %>
</head>
<body>
    <% include ../partials/header %>
    

  <div class="container-fluid">
    <div class="">
        <% include ../partials/images/mainImage %>
      <div class="page-header">
        <h1><%= title %></h1>
      </div>
        <% if(user) {%>
          <h2>
          <img src="<%= user.facebook.picture.data.url %>" alt="s" class="img-circle">
            
          </h2>
        <%} %>    
      <p class="lead">Welcome to <%= title %></p>
  </div>

    <% include ../partials/footer %>
    <% include ../partials/javascript %>
    <script>

    </script>
</body>
</html>
